#include <stdio.h>

int main (void)
{
  char letter;
  for (letter = 'a'; letter <= 'z'; letter++)
    putchar (letter);
  putchar('\n');

  /*UPPEERCASE*/

  for (cap_letter = 'A'; cap_letter <= 'Z'; letter++)
    putchar (letter);
  putchar('\n');
  
  return (0);
}
  
